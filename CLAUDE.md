# Project guidance

Project guidance for agents lives under `.agents/skills/`. Read the relevant skill(s) before working on this repo:

- `.agents/skills/plugin/` — shared build, test, conventions, and Wally deps
- `.agents/skills/<plugin>/` — per-plugin architecture (e.g. `resizealign`, `gapfill`, `materialflip`, `adhoc`, `polymap`, `roadhelper`, `ropetool`)
